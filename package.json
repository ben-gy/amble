{
  "name": "amble",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "An unhurried journey down one long road — the traveller furthest behind always goes next.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@ben-gy/game-engine": "github:ben-gy/gh-game-engine#v1.3.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0",
    "jsdom": "^25.0.0"
  }
}
